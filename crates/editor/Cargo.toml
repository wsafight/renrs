[package]
name = "renrs-editor"
version = "0.1.0-rc.1"
edition = "2024"
rust-version = "1.88"
license = "MIT"

[dependencies]
renrs-compiler = { path = "../compiler" }
renrs-project = { path = "../project" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
url = "2.5"

[dev-dependencies]
tempfile = "3.22"

[lints]
workspace = true
