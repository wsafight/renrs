[package]
name = "renrs-web"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
renrs-runtime = { path = "../runtime" }
serde_json = "1"
wasm-bindgen = "=0.2.121"

[lints.rust]
unsafe_code = "forbid"
