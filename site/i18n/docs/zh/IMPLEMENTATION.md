# 实施状态

跟踪 2026-09-06 要求的 P0 到 P2 工作。已勾选表示包含实现和相关验证；外部验收仍单独标明。
这是带日期的交付记录，不是实时项目摘要。当前范围决定见 [当前开发进度](NEXT_PRODUCT_WORK.md)，
实际使用入口见 [快速开始](QUICKSTART.md)。

## P0

- [x] 拒绝有歧义的跨版本存档位置；保留锚定的 call 和回滚。
- [x] 恢复存档和回滚检查点时初始化缺失的 default。
- [x] 在加载、打包和监听之间共用项目资源纳入规则。

P0 验证：本地 114 项测试、格式化和严格 Clippy 通过。在这份历史记录对应的版本中，快照 v7 用调用点 ID 作为返回地址，
保留动态 label 参数作用域，并复用共享值；当前 v8 兼容读取 v7。更旧快照格式会被拒绝；内容更新仅恢复显式位置，
指纹变化时拒绝自动位置。

## P1

- [x] 在覆盖、读档和退出时保护进度；周期和章节自动存档。
- [x] 后台存档操作、轻量元数据、页码、截图和备注。
- [x] 编辑器磁盘/缓冲索引、引用、诊断和宿主回归测试。
- [x] 音频完成、错误、重放和可配置默认值。
- [x] 共用播放器 UI 本地化和无障碍偏好。
- [x] 工具发现、项目创建和本地桌面发行验证。
- [x] 在 demo 和合成中型工作负载上测量帧时间和内存。

## P2

- [x] 交互剧情图、状态查看、断点和路线覆盖。
- [x] 持久成就、图鉴、结局和回滚屏障。
- [x] 串行时间线、dissolve、无声音频帧片段，以及桌面开关/输入控件。
- [x] 签名/公证命令、签名资源差量和本地商店文件生成。
- [x] 使用共享 Rust 运行时的 Web 播放器；桌面/手机 Chromium 验收。
- [x] 编译、存档/回滚、原生/Web 渲染和 LSP 共用可排序的命名立绘层。

这一历史迭代交付了有界子集。后续产品工作增加了同步视频、并行轨道、共用 Web 界面、
键盘存档工具、NVL 和 Capacitor 移动构建。当前状态和限制另见
[产品优化记录](PRODUCT_UPGRADES.md)。完整 ATL、完整 UI 翻译、原生 Rust 移动渲染、
商店 SDK 和云存档仍在该子集之外。外部签名/公证和远程平台验收下面仍未勾选。

后续性能工作增加了流式音视频、后台编译、有界解码和紧凑 Web 状态。见
[性能测量](PERFORMANCE.md)。

共享 Rust 实现验证：拆成 syntax、model、compiler、runtime、project、editor 和 web crate 后
125 项测试通过。原生播放器和 CLI 入口仍在根包。常用 UI 标签已本地化；部分诊断和
收藏文案仍是英文。

本地验收还通过了六条 Chromium 流程、VS Code 扩展宿主、原生 demo 和长文本/自定义控件
截图、打包的 macOS 应用启动，以及两次 300 帧 release 测量。见 [本地验证](VALIDATION.md)。
当时的 Web 预览从 `target/web-final` 提供，地址 http://127.0.0.1:4174/。

## 外部验收

- [ ] 由所有者提供的真实中型项目。
- [ ] 在发行方目标桌面平台完成本地打包播放器验收。
- [ ] 由发行方提供的签名身份、公证和商店凭据。
