# Demo asset notes

The images and WAV files in this directory are generated by
`examples/generate_demo_assets.rs` and are released with RenRS under the MIT
license. They do not contain or derive from Ren'Py tutorial assets.

Regenerate them from the repository root with:

```sh
cargo run --example generate_demo_assets -- demo
```
