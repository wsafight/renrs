# 顺序优化验收记录

本轮按以下顺序交付，每项以代码、测试和文档为验收依据。

- [x] 玩家体验：完整历史滚动、即时语言选择、手动/快速/自动存档浏览、长对白分页。97 项自动测试通过；窗口截图在最后阶段统一验收。
- [x] 性能：存档列表缓存、64 KiB 缓冲区流式打包、后台图片解码、纹理/音频缓存预算、资源热重载。100 项自动测试通过。
- [x] 创作入口：`renrs-init` 及 VS Code 的 LSP/诊断/预览/运行/构建入口；本地 VSIX 已重建并包含许可证，尚未在 VS Code 窗口中验收。
- [x] 剧情调试：`renrs-debug inspect/record/replay/test/explore`；双结局、缺失/多余选择、状态断言、探索上限和结束后存档恢复测试通过。
- [x] 界面定制：`screens.json` 支持声明式文本/图片/按钮/滑块/列表和行列布局、受控动作；106 项自动测试通过。
- [x] 规模与发行验证：100 章、10,000 段对白的 release 基准；109 项自动测试、严格 Clippy、fmt/check 通过；macOS 两种窗口尺寸、目录/归档及 release 发行包启动验收通过。

完整命令、实测结果和边界见 [规模与运行验证](VALIDATION.md)。本地输出：

- `target/validation/benchmark.json`：当前发布模式基线，编译约 56–84 ms，10,102 次剧情交互约 52 ms。
- `target/validation/demo-*`、`visual-*`：内置与自定义界面的 1280x720/800x600 窗口截图。
- `target/validation/package-800`、`package-release-800`：从 `/private/tmp` 启动发行包的截图与快照恢复结果。
- `target/validation/release-distribution/`：可直接启动的发布模式示例，含引擎与字体许可证。
- `editors/vscode-renrs/renrs-0.1.0-rc.1.vsix`：可安装的编辑器扩展。

六次原生运行共输出 81 张 PNG，报告均为通过；长对白第二页、12 个长选项、历史首尾、中文切换、
自定义滑块/列表与手动/快速/自动存档均有截图。最后补齐了自定义历史缓存、超长标签排版、
容器样式误用校验、存档缓存 Send/Sync 属性，以及与工作目录无关的发行包查找。

创作入口见 [工具链](TOOLING.md)，路线断言见 [剧情调试](DEBUGGING.md)，界面格式见
[声明式界面](SCREENS.md)。模板/路线、VSIX 和窗口检查现由本地分层门禁执行；GitHub Actions
只保留文档站发布。远程三平台验收和人工音频试听未在本轮实际执行。

性能边界：纹理缓存目标 256 MiB，音频缓存按解码体积估算、目标 64 MiB；当前舞台和正在播放的音频不会被强制淘汰，
因此活跃资源本身超过预算时仍可能超出目标。图片后台队列有界；GPU 上传和现有音频后端解码仍在主线程。
存档 UI 不再逐帧读盘，外部文件变化最多一秒可见，显式读档始终重新校验。
本机大存档写入约 144 ms、六槽首次读取约 263 ms，均仍同步执行；对白页码尚未进入存档。
